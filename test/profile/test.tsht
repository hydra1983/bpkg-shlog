#!/usr/bin/env tsht

set -e

SHLOG_DATE_FORMAT="%(%T)T"
source "../../dist/shlog"

# echo "${!SHLOG_OUTPUTS[@]}"
# shlog -d SHLOG_FILE
# shlog -d SHLOG_LEVELS
# shlog -d SHLOG_COLORIZE
# shlog -d SHLOG_OUTPUTS
# SHLOG_OUTPUTS[file]=trace
# # exit
# time for i in $(seq 1);do
    # shlog -l trace foo
    # shlog -l debug foo
    # shlog -l info foo
    # shlog -l warn foo
    # shlog -l error foo
# done
